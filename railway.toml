[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "NODE_ENV=production JWT_SECRET=flymasters-admin-dev-secret npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
