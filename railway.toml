[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "JWT_SECRET=flymasters-admin-dev-secret npm run start:prod"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
